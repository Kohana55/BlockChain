<UserControl x:Class="BlockChain.Views.MainView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:BlockChain.Views"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>

        <Grid.RowDefinitions>
            <RowDefinition Height="*"/>
            <RowDefinition Height="50"/>
        </Grid.RowDefinitions>

        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>
        
        <local:AddBlockView x:Name="_AddBlockView" Grid.Row="0" Grid.Column="0" Grid.ColumnSpan="2"/>
        <local:BlockChainView x:Name="_BlockChainView" Grid.Row="0" Grid.Column="0" Grid.ColumnSpan="2"/>

        <Button Content="Transaction Screen" Grid.Row="1" Grid.Column="0" Command="{Binding TransactionScreenCommand}"/>
        <Button Content="Ledger Screen" Grid.Row="1" Grid.Column="1" Command="{Binding LedgerScreenCommand}"/>
    </Grid>
</UserControl>
